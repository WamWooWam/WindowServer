{
  "name": "notepad",
  "version": "1.0.0",
  "description": "Notepad",
  "main": "dist/notepad.js",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "@window-server/sdk": "workspace:sdk",
    "@window-server/user32": "workspace:user32",
    "@window-server/gdi32": "workspace:gdi32",
    "@window-server/kernel32": "workspace:kernel32",
    "@window-server/ntdll": "workspace:ntdll"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@window-server/build-tools": "workspace:build-tools"
  },
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -c -w"
  }
}
